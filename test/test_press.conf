@init_chk_conf          1
@buf_size_min           0
@buf_size_max           2MB
@rotate_lock_file       /tmp/xlog.lock

&default                "$d(%F %X) | $p:$F:$L | $m$n"

*.*             	"press.log",1K;
