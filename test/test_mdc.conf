@ignore_error_format_rule	0
@buf_size_min			1024
@buf_size_max			2MB
@rotate_lock_file		/tmp/zlog.lock

&null			""
&simple			"%m%n"
&text			"text"
&default		"%d(%F %X.%ms) %-6P (%c:%F:%L) (M1:%M(myname)) (M2:%M(yourname)) - %m%n"

*.*		>stdout;
